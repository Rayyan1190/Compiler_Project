fn int my_fn(int x, float y) {
    // line comment
    /* block
       comment
       */
   
    string s = "hi\t\"ok\"\n";
    bool b = x >= 10 && y != 0.0;
    int x=$abc;
    if (b) { return x; } else { return 0; }
    int a = 1 + 2 * 3 - 4 / 2 % 2;
    
    x = x << 1; y = y >> 1.0;
    int arr[3]; arr[0] = 40;
}
